{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile within the CRM application, storing essential information beyond authentication credentials for display, assignment, and team management purposes.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity, typically corresponding to the user's authentication ID (e.g., Firebase Auth UID)."
        },
        "email": {
          "type": "string",
          "description": "The primary email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "The public display name of the user."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the CRM system, defining access levels and responsibilities."
        },
        "photoUrl": {
          "type": "string",
          "description": "URL to the user's profile picture.",
          "format": "uri"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the user profile was last updated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "displayName",
        "createdAt",
        "updatedAt"
      ]
    },
    "Lead": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Lead",
      "type": "object",
      "description": "Represents a potential customer or sales opportunity, tracking their journey through the sales pipeline.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Lead entity."
        },
        "ownerUid": {
          "type": "string",
          "description": "Reference to UserProfile.id, indicating the user who owns or is assigned this lead."
        },
        "name": {
          "type": "string",
          "description": "The full name of the lead contact."
        },
        "email": {
          "type": "string",
          "description": "The primary email address of the lead.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The primary phone number of the lead."
        },
        "company": {
          "type": "string",
          "description": "The company associated with the lead."
        },
        "status": {
          "type": "string",
          "description": "The current stage of the lead in the sales pipeline.",
          "enum": [
            "new",
            "contacted",
            "qualified",
            "proposal",
            "negotiated",
            "won",
            "lost"
          ]
        },
        "dealValue": {
          "type": "number",
          "description": "The estimated monetary value of the potential deal with this lead."
        },
        "source": {
          "type": "string",
          "description": "The origin or channel through which the lead was acquired (e.g., 'Website', 'Referral', 'Cold Call')."
        },
        "tags": {
          "type": "array",
          "description": "An array of descriptive tags or keywords associated with the lead.",
          "items": {
            "type": "string"
          }
        },
        "nextFollowUpAt": {
          "type": "string",
          "description": "Timestamp for the next scheduled follow-up or contact with the lead.",
          "format": "date-time"
        },
        "lastContactAt": {
          "type": "string",
          "description": "Timestamp of the most recent interaction or contact with the lead.",
          "format": "date-time"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the lead record was initially created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp when the lead record was last updated.",
          "format": "date-time"
        },
        "customFields": {
          "type": "object",
          "description": "A flexible object for storing additional, user-defined fields specific to the lead."
        },
        "notesCount": {
          "type": "number",
          "description": "Denormalized count of notes associated with this lead."
        },
        "callsCount": {
          "type": "number",
          "description": "Denormalized count of calls logged for this lead."
        }
      },
      "required": [
        "id",
        "ownerUid",
        "name",
        "status",
        "createdAt",
        "updatedAt"
      ]
    },
    "Activity": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Activity",
      "type": "object",
      "description": "Records an interaction or significant event related to a specific lead, contributing to the lead's history and timeline.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Activity entity."
        },
        "leadId": {
          "type": "string",
          "description": "Reference to Lead.id, indicating which lead this activity pertains to."
        },
        "ownerUid": {
          "type": "string",
          "description": "Reference to UserProfile.id, indicating the user who logged this activity."
        },
        "type": {
          "type": "string",
          "description": "The category of the activity.",
          "enum": [
            "note",
            "call",
            "status_change",
            "tag_added",
            "event_created"
          ]
        },
        "content": {
          "type": "string",
          "description": "The detailed description or content of the activity."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the activity was recorded.",
          "format": "date-time"
        },
        "oldStatus": {
          "type": "string",
          "description": "The previous status of the lead."
        },
        "newStatus": {
          "type": "string",
          "description": "The new status of the lead."
        }
      },
      "required": [
        "id",
        "leadId",
        "ownerUid",
        "type",
        "content",
        "createdAt"
      ]
    },
    "CalendarEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CalendarEvent",
      "type": "object",
      "description": "Represents a scheduled event, meeting, or reminder.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Event entity."
        },
        "ownerUid": {
          "type": "string",
          "description": "Reference to UserProfile.id, indicating the user who created or owns this event."
        },
        "leadId": {
          "type": "string",
          "description": "Reference to Lead.id."
        },
        "title": {
          "type": "string",
          "description": "A concise title or subject for the event."
        },
        "description": {
          "type": "string",
          "description": "A detailed description or agenda for the event."
        },
        "startAt": {
          "type": "string",
          "description": "The starting date and time of the event.",
          "format": "date-time"
        },
        "endAt": {
          "type": "string",
          "description": "The ending date and time of the event.",
          "format": "date-time"
        },
        "allDay": {
          "type": "boolean",
          "description": "A boolean indicating if the event is an all-day event."
        },
        "eventType": {
          "type": "string",
          "description": "The category or nature of the event.",
          "enum": [
            "follow-up",
            "meeting",
            "task",
            "reminder"
          ]
        },
        "status": {
          "type": "string",
          "description": "The current status of the event.",
          "enum": [
            "scheduled",
            "completed",
            "cancelled"
          ]
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the event record was initially created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "ownerUid",
        "title",
        "startAt",
        "eventType",
        "createdAt"
      ]
    },
    "TrainingMaterial": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "TrainingMaterial",
      "type": "object",
      "description": "Represents a training resource or document for sales reps.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the TrainingMaterial."
        },
        "userId": {
          "type": "string",
          "description": "The user who uploaded the material."
        },
        "title": {
          "type": "string",
          "description": "The title of the material."
        },
        "subject": {
          "type": "string",
          "description": "The category (e.g., Cold Calling)."
        },
        "content": {
          "type": "string",
          "description": "The content or body of the training material."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the material was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "title",
        "subject",
        "content",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "google",
      "anonymous"
    ]
  },
  "firestore": {
    "training_materials": {
      "schema": {
        "$ref": "#/backend/entities/TrainingMaterial"
      },
      "description": "Stores organizational training resources accessible by all team members."
    },
    "leads": {
      "schema": {
        "$ref": "#/backend/entities/Lead"
      },
      "description": "Stores all lead records for the organization."
    },
    "activities": {
      "schema": {
        "$ref": "#/backend/entities/Activity"
      },
      "description": "Stores activity logs and interaction history for leads."
    },
    "calendarEvents": {
      "schema": {
        "$ref": "#/backend/entities/CalendarEvent"
      },
      "description": "Stores team calendar events and follow-up reminders."
    },
    "userProfiles": {
      "schema": {
        "$ref": "#/backend/entities/UserProfile"
      },
      "description": "Stores user-specific profile data."
    },
    "teamSettings": {
      "schema": {
        "type": "object",
        "properties": {
          "totalTeamQuota": {
            "type": "number"
          },
          "monthlyTarget": {
            "type": "number"
          }
        }
      },
      "description": "Global settings for team quotas and targets."
    }
  }
}
